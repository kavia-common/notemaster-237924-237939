# Database (provided by notes_database container)
POSTGRES_URL=

# JWT auth
JWT_SECRET=
JWT_ISSUER=notemaster
JWT_AUDIENCE=notemaster-web
ACCESS_TOKEN_TTL_MINUTES=10080

# CORS (comma-separated origins or "*")
CORS_ALLOW_ORIGINS=*
